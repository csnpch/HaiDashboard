export interface pageInterface {
    pagination: {
        limit_size: number,
        current_page: number,
        next_page: number,
        previous_page: number,
        page_size: number
    }
}